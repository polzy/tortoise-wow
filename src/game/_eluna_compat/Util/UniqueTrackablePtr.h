#pragma once
// MCWoW Eluna compat stub — feature not present in vanilla, but Eluna's headers
// reference the include path unconditionally. Empty stub avoids compile errors;
// any code path that actually uses these symbols is gated behind expansion macros
// (CLASSIC, TBC, WOTLK) so it never executes at runtime.
